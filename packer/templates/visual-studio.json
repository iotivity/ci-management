{
  "variables": {
    "stack_tenant": null,
    "stack_user": null,
    "stack_pass": null,
    "stack_network": null,
    "base_image": null,
    "cloud_user": null,
    "distro": null,
    "cloud_user_data": null,
    "winrm_username": null,
    "winrm_password": null,
    "jenkins_username": null,
    "jenkins_password": null,
    "admin_username": null,
    "admin_password": null,
    "arch": null

  },
  "builders": [
    {
      "type": "openstack",
      "identity_endpoint": "https://auth.vexxhost.net/v2.0/",
      "tenant_name": "{{user `stack_tenant`}}",
      "username": "{{user `stack_user`}}",
      "password": "{{user `stack_pass`}}",
      "region": "ca-ymq-1",
      "ssh_username": "{{user `cloud_user`}}",
      "image_name": "{{user `distro`}} - {{user `toolchain`}} - {{isotime \"20060102-1504\"}}",
      "source_image_name": "{{user `base_image`}}",
      "flavor": "v1-performance-4",
      "availability_zone": "ca-ymq-2",
      "communicator": "winrm",
      "winrm_timeout": "10000s",
      "winrm_username": "admin",
      "networks": [
        "{{user `stack_network`}}"
      ],
      "user_data_file": "{{user `cloud_user_data`}}"
    }
  ],
  "provisioners": [
    { "type": "powershell",
      "valid_exit_codes": [0, 3010],
      "scripts": [
	  "provision/basebuild/install-{{user `toolchain`}}.ps1"
      ]
    },
    { "type": "windows-restart" },
    { "type": "powershell",
      "scripts": [
	  "provision/basebuild/system_reseal.ps1"
      ]
    }

  ]
}
