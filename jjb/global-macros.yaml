# Global Macros

## Parameters
- parameter:
    name: gerrit-branch
    parameters:
        - string:
            name: GERRIT_BRANCH
            default: 'master'
            description: "JJB configured GERRIT_BRANCH parameter"

- parameter:
    name: gerrit-project
    parameters:
        - string:
            name: GERRIT_PROJECT
            default: '{project}'
            description: "GERRIT_PROJECT parameter if not given by trigger"

- parameter:
    name: gerrit-refspec
    parameters:
        - string:
            name: GERRIT_REFSPEC
            default: 'refs/heads/master'
            description: "GERRIT_REFSPEC parameter if not given by trigger"

## SCMs
- scm:
    name: gerrit-branch-scm
    scm:
        - git:
            credentials-id: '{credentials-id}'
            url: 'ssh://iotivity-jobbuilder@gerrit.iotivity.org:29418/iotivity'
            refspec: '$GERRIT_REFSPEC'
            branches:
                - 'origin/$GERRIT_BRANCH'
            skip-tag: true
            wipe-workspace: true
            choosing-strategy: 'gerrit'

- scm:
    name: gerrit-ci-management-scm
    scm:
        - git:
            credentials-id: '{credentials-id}'
            url: 'ssh://iotivity-jobbuilder@gerrit.iotivity.org:29418/ci-management'
            refspec: '$GERRIT_REFSPEC'
            branches:
                - 'origin/$GERRIT_BRANCH'
            skip-tag: true
            wipe-workspace: true
            choosing-strategy: 'gerrit'

- scm:
    name: gerrit-iotivity-upnp-scm
    scm:
        - git:
            credentials-id: '{credentials-id}'
            url: 'ssh://iotivity-jobbuilder@gerrit.iotivity.org:29418/iotivity-upnp-bridge'
            refspec: '$GERRIT_REFSPEC'
            branches:
                - 'origin/$GERRIT_BRANCH'
            skip-tag: true
            wipe-workspace: true
            basedir: 'iotivity-upnp-bridge'
            choosing-strategy: 'gerrit'

## Triggers
- trigger:
    name: gerrit-trigger-patch-submitted
    triggers:
        - gerrit:
            server-name: 'iotivity'
            silent: '{obj:silent}'
            trigger-on:
                - patchset-created-event:
                    exclude-drafts: 'false'
                    exclude-trivial-rebase: 'false'
                    exclude-no-code-change: 'true'
                - draft-published-event
                - comment-added-contains-event:
                    comment-contains-value: 'verify all'
                - comment-added-contains-event:
                    comment-contains-value: 'reverify'
            projects:
                - project-compare-type: 'ANT'
                  project-pattern: '{project}'
                  branches:
                    - branch-compare-type: 'ANT'
                      branch-pattern: '{branch}'

- trigger:
    name: gerrit-trigger-ref-updated
    triggers:
        - gerrit:
            server-name: 'iotivity'
            silent: '{obj:silent}'
            silent-start: 'true'
            trigger-on:
                - ref-updated-event
                - comment-added-contains-event:
                    comment-contains-value: 'remerge'
            projects:
                - project-compare-type: 'ANT'
                  project-pattern: '{project}'
                  branches:
                    - branch-compare-type: 'ANT'
                      branch-pattern: '{branch}'

- trigger:
    name: gerrit-trigger-tag
    triggers:
      - gerrit:
          server-name: 'iotivity'
          trigger-on:
            - ref-updated-event
          projects:
            - project-compare-type: 'ANT'
              project-pattern: '{project}'
              branches:
                - branch-compare-type: 'ANT'
                  branch-pattern: 'refs/tags/**'

## Builders
- builder:
    name: ci-management-check-unicode
    builders:
        - shell: |
            $WORKSPACE/scripts/check-unicode.sh jjb/
