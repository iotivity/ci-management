---
- view:
    name: '00-Empty View'
    description: >
      Empty job view. This is used as the default landing view to keep the
      Jenkins UI responding better while a) under high load and b) when there
      are a lot of jobs defined in the system'
    view-type: list
    filter-executors: false
    filter-queue: false
    recurse: false
    regex: ''
- view:
    name: IoTivity
    description: 'List of IoTivity jobs'
    view-type: list
    filter-executors: false
    filter-queue: false
    columns:
      - status
      - weather
      - job
      - last-success
      - last-failure
      - last-duration
      - build-button
    recurse: false
    regex: '^iotivity-.*'
- view:
    name: IoTivity-constrained
    description: 'List of IoTivity constrained jobs'
    view-type: list
    filter-executors: false
    filter-queue: false
    columns:
      - status
      - weather
      - job
      - last-success
      - last-failure
      - last-duration
      - build-button
    recurse: false
    regex: '^iotivity-constrained-.*'
- view:
    name: IoTivity-merge
    description: 'List of IoTivity merge jobs'
    view-type: list
    filter-executors: false
    filter-queue: false
    columns:
      - status
      - weather
      - job
      - last-success
      - last-failure
      - last-duration
      - build-button
    recurse: false
    regex: '^iotivity-merge-.*'
- view:
    name: IoTivity-upnp-bridge
    description: 'List of IoTivity upnp bridge jobs'
    view-type: list
    filter-executors: false
    filter-queue: false
    columns:
      - status
      - weather
      - job
      - last-success
      - last-failure
      - last-duration
      - build-button
    recurse: false
    regex: '^iotivity-upnp-bridge-.*'
- view:
    name: ci-managment
    description: 'List of ci-managment jobs'
    view-type: list
    filter-executors: false
    filter-queue: false
    columns:
      - status
      - weather
      - job
      - last-success
      - last-failure
      - last-duration
      - build-button
    recurse: false
    regex: '^ci-managment-.*'
