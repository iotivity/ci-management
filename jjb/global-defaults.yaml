# Global Jenkins Defaults
- defaults:
    name: global

    build-days-to-keep: -1
    build-num-to-keep: 300
    build-artifact-days-to-keep: -1
    build-artifact-num-to-keep: -1

    silent: false
    branch: 'master'
    refspec: 'refs/heads/master'

    ssh-credentials: '6ba265b5-4270-46f3-aeaa-44b789a5d7a9'
