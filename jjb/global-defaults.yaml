# Global Jenkins Defaults
- defaults:
    name: global

    build-discarder:
        daysToKeep: 60
        numToKeep: 100
        artifactDaysToKeep: 30
        artifactNumToKeep: 100

    silent: false
    branch: 'master'
    refspec: 'refs/heads/master'
    gerrit-branch: '**'

    ssh-credentials: '6ba265b5-4270-46f3-aeaa-44b789a5d7a9'
